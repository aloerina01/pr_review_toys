{
  "scripts": {
    "start": "parcel start sample.html",
    "build": "rm -rf public/dist/* && parcel build src/index.tsx -d public/dist --out-file content_script.js --no-minify",
    "build:prod": "rm -rf public/dist/* && parcel build src/index.tsx -d public/dist --out-file content_script.js"
  },
  "dependencies": {
    "preact": "^10.5.14"
  },
  "devDependencies": {
    "parcel-bundler": "^1.9.4",
    "typescript": "^4.3.5"
  }
}
